import { FilterProductListPipe } from './filter-product-list.pipe';

describe('FilterProductListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductListPipe();
    expect(pipe).toBeTruthy();
  });
});
